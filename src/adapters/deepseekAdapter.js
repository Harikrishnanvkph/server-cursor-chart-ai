import fetch from 'node-fetch'

/**
 * DeepSeek adapter placeholder. Provides consistent interface with other adapters
 * but intentionally does not call the upstream API yet.
 */

export async function callDeepSeekAPI({ prompt, model, signal }) {
  throw new Error('DeepSeek API integration is not yet implemented.');
}

